import os


def getWeather(city: str) -> str:
    """Get the current weather for a given city.

    Args:
        city: Name of the city to check weather for.
    """
    # TODO p4 call a real weather API here.
    # weatherApiKey = os.getenv("WEATHER_API_KEY")  # only needed for OpenWeatherMap
    return f"(not implemented yet) would fetch real weather for '{city}'"